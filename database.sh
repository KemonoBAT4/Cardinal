# TODO: implement this script

# NOTE: when using this script, it can interact with the database
# the name of the wanted application must be passed as the first argument

# NOTE: if the second argument is --reset or similar (a reset command)
# then the script has to completely reset the database for that application ONLY

# NOTE: if the second argument is --migrate or similar (a migrate command)
# then the script has to migrate the database for that application ONLY

# NOTE: if the second argument is --shell or similar (a seed command)
# then the script has enter the database's shell so the user can perform commands there

# NOTE: any other argument will be passed or converted based on what 
# the application needs to execute this script
